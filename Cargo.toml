[workspace]
members = [
    "core",
    "chat_manager",
    "stream_finder",
    "detector",
    "alkonost",
    "ui"
]

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
opt-level = 'z'